#if( $result == '-1' )
<div class="alert alert-danger">
<h4>Result</h4>
<p>$attr-joint is not a key in $relation.name</p>
#elseif( $result == '0' )
<div class="alert alert-warning">
<h4>Result</h4>
<p>$attr-joint is a key, but not a minimal in $relation.name</p>
#else
<div class="alert alert-success">
<h4>Result</h4>
<p>$attr-joint is a minimal key in $relation.name</p>
#end

</div>