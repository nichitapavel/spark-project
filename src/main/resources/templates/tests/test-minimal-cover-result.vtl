#if( $result )
  <div class="alert alert-success">
    <h4>Result</h4>
    <p>$fdjoint.name is Minimal</p>
  </div>
#else
  <div class="alert alert-danger">
    <h4>Result</h4>
    <p>$fdjoint.name is not Minimal</p>
  </div>
#end